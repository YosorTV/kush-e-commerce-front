import { PageLayout } from '@/components/layouts';

import { PageProps } from '@/types/app/page.types';

export default async function Collection({ params }: PageProps) {
  return <PageLayout className='mt-16'>text</PageLayout>;
}
